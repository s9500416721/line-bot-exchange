services:
  - type: web
    name: line-bot-exchange
    env: python
    pythonVersion: 3.11.11
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn main:app"
    plan: free
